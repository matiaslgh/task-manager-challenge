# Start backend
- ```cd src/backend```
- ```mvn spring-boot:run``` (maven is required)
> Runs on port 8080

# Start frontend
- ```cd src/frontend```
- ```npm install``` (node is required)
- ```npm start```
> Runs on port 3000
